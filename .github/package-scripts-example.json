{
  "name": "node-mysql-express-example",
  "scripts": {
    "test": "mocha test/**/*.js --timeout 5000",
    "test:coverage": "nyc mocha test/**/*.js --timeout 5000",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "migrate": "node migrations/migrate.js",
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  "devDependencies": {
    "eslint": "^8.0.0",
    "prettier": "^2.8.0",
    "mocha": "^10.0.0",
    "nyc": "^15.1.0",
    "chai": "^4.3.0",
    "supertest": "^6.3.0",
    "nodemon": "^2.0.0"
  }
}